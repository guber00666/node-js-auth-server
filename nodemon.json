{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "ts-node ./src/server/index.ts",
  "events": {
    "restart": "kill-port 3000",
    "crash": "kill-port 3000"
  }
}